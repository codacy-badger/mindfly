#server.port=9090

# evn setting
spring.profiles.active=test

# mybatis config
mybatis.config-location=classpath:config/mybatis-config.xml
mybatis.mapper-locations=classpath:mapper/**/*.xml
mybatis.type-aliases-package=slancer.mindfly.service.account.entity
mybatis.mappers-package=slancer.mindfly.service.account.dao
